let numbers = [1,3,4,5,6,7];
console.log(numbers.includes(2));

const list = ['Oscar', 'David', 'Tibe', 'Julian', 'Lukas'];
console.log(list.includes('lukas'));

console.log(list.map(name => name.toLowerCase()));